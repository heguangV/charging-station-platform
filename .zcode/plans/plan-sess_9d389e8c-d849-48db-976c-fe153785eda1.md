重启所有端（含安卓真机），不动 APK

背景结论：你已经在 build-ncs 里构建过了（ncs_server 14:58、ncs_admin 14:58、ncs_user 14:49，均晚于最后一次源码修改 14:56），systemd 与 dev.sh 用的正是这套二进制。apps/mobile 无改动，APK 无需重建。

步骤：

1. 增量构建保底：`cmake --build build-ncs --parallel` —— 大概率几秒内确认"已是最新"；若有文件在你上次构建后又改动会自动补编。若编译失败，如实报告并停止，不会用旧二进制重启。

2. 重启三端 + 服务端：`scripts/dev.sh restart`（停掉 ncs_admin、ncs_user、Web 大屏和 systemd 的 ncs-server，再全部拉起）。

3. 验证：
   - `scripts/dev.sh status` 确认四个组件运行中；
   - 健康检查 http://127.0.0.1:18443/api/v1/system/health/ready 返回 UP；
   - 查看 /tmp/ncs-dev/ 下客户端启动日志有无报错。

4. 安卓端恢复联调：服务端重启会断开手机连接，执行 `apps/mobile/run_android.sh`（不带 --install）重建 `adb reverse tcp:18443` 并把应用拉回前台。