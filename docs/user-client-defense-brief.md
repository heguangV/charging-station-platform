# PE1 Qt 用户客户端答辩说明

| 项目 | 内容 |
| --- | --- |
| 负责成员 | prayer666 |
| 负责范围 | `apps/user`：Qt Widgets 车主用户端 |
| 需求依据 | [需求规格说明书](01-requirements-specification.md) 的 `UC-U-*`；接口以 [database-api.md](database-api.md) 为准 |
| 当前阶段 | 正常模式已接入异步 REST；Mock 仅保留为显式演示与回归模式；真实 HTTP 业务闭环已完成本机联调 |

## 给组长：PPT 可直接采用的概要

### 负责内容与阶段成果

PE1 负责 Linux + Qt Widgets 车主用户客户端，已完成可演示的用户主流程：

```text
手机号验证登录 → 查询附近电站 → 查看/选择空闲电桩 → 预约
→ 开始充电 → 查看电量、费用与 SoC → 结束结算 → 订单与账户查看
```

当前界面采用 420×760 的移动端交互模拟，已具备首页/订单/我的三项 Dock、站点卡片、选桩反馈、充电仪表、订单空状态、账户充值，以及加载、空、失败和可自动淡出的 Toast 提示状态。正常启动使用真实服务端；显式传入 `--mock-scenario` 时可用 Mock 数据完成离线录屏、截图和异常场景回归。

### 需求分析结论

用户端的核心不是“展示电桩数据”，而是帮助车主以尽量少的操作完成一次可信的充电闭环。需求重点可归纳为：

1. **找得到**：基于当前位置/区域查询电站，按距离展示价格、空闲桩数和距离；
2. **选得对**：只允许选择空闲桩，明确展示快慢充、功率和故障状态；
3. **充得安心**：预约、倒计时、开始、实时进度和结算形成受约束状态链；
4. **账算得清**：展示累计电量、费用、SoC、订单和余额，并在余额不足、预约过期、设备不可用时给出用户可理解的反馈；
5. **服务可持续**：客户端只访问 HTTPS REST，数据和事务由服务端统一负责；Mock 仅用于显式离线演示和回归。

### 技术选型与设计思路

| 项目 | 方案 | 说明 |
| --- | --- | --- |
| 客户端 | C++17 + Qt 6.2 Widgets | 与 Ubuntu 运行环境和团队 CMake 工程一致，适合桌面端模拟手机交互 |
| 视觉与交互 | QSS + 自有 Qt 小组件 | 统一能源绿主题、卡片、细滚动条、Dock 微动效、SoC 仪表与 Toast；不引入不稳定 UI 库 |
| 页面架构 | Widget/Page → Service → 数据源 | 页面只负责交互和展示，不直接写 SQL、不手拼 HTTP/JSON |
| 演示与回归 | `MockUserClientService` | 仅在 `--mock-scenario` 下离线走通场景，不替代正常业务数据 |
| 正式对接 | `UserApi` + `ApiClient` + HTTPS REST | `QNetworkAccessManager` 异步请求，统一 Token、超时、错误码、请求 ID 和幂等键 |

### 当前边界（答辩时应如实说明）

- 当前窗口默认连接真实 REST；隔离服务端上的真实 HTTP 账户充电闭环已验证，但 Qt 图形界面逐页点击录屏尚未作为端到端证据执行；
- Mock 仅在 `--mock-scenario` 下启用，用于离线演示和回归，不会在正常模式自动回退；
- 金额扣减、设备占用、订单状态迁移、并发抢桩和 SQLite 操作均在服务端，客户端不直连数据库。

### 创新规划（答辩表述为“下一阶段”）

围绕“更省时、更省钱、更低碳”，后续计划由后端/ML 输出可解释数据，客户端呈现：智能选桩推荐（空闲数、距离、价格、功率、预计等待）、目标 SoC/预算预估、低碳低峰充电建议和断线后的活动订单恢复。

## 1. 我对用户端需求的理解

用户端不是管理后台，而是车主在出发前和充电过程中的移动端交互模拟。目标是让用户在 420×760 的桌面窗口中完成完整闭环：

```text
手机号登录 → 查找附近站点 → 查看并选择电桩 → 预约
→ 开始充电 → 查看实时进度 → 结束结算 → 查看订单/余额
```

关键业务约束：用户不能同时创建多个未完成流程；只有空闲电桩可选；预约有倒计时且可取消；充电数据按秒刷新；结算需要二次确认；余额不足、桩不可用、未完成订单和网络失败均需要给用户可理解的反馈。

## 2. 分工与边界

| 成员/模块 | 职责 | 用户端关系 |
| --- | --- | --- |
| PE1（prayer666） | Qt 用户端页面、交互、Mock、REST 客户端封装 | 调用 `/api/v1/user/*`，不直接访问数据库 |
| PE2 | Qt 管理端 | 与用户端共享后端契约，不共享页面逻辑 |
| PE3 | Crow 服务、鉴权、订单/计费事务、SQLite、设备模拟 | 提供用户端事实数据与 REST 响应 |
| PE4 | 大屏、ML、地图等扩展 | 用户端可消费其后端输出，不耦合模型实现 |

客户端不实现原生 TCP/UDP，也不打开 SQLite；业务真相、金额计算、订单状态变迁和并发控制由后端负责。

## 3. 技术选型及理由

| 技术 | 用途 | 选择理由 |
| --- | --- | --- |
| C++17 + Qt 6.2 Widgets | 桌面模拟手机端 UI | 当前 Ubuntu 环境可运行，窗口、信号槽、定时器和 QSS 足以支持验收演示 |
| CMake | 构建 | 与团队工程统一，便于多端目标集成 |
| QNetworkAccessManager | HTTPS REST 异步请求 | Qt 原生、非阻塞 GUI、可统一处理 Token/超时/错误 |
| MockUserClientService | 离线演示数据源 | 后端不可用时仍可完成录屏、截图和主流程演示 |
| QTimer | 倒计时与进度刷新触发 | Mock 中驱动演示；REST 模式中应每秒异步请求 progress |
| QSS + 自绘组件 | 主题、SoC 环形仪表、Dock 动效 | 不引入不稳定的外部 UI 库，保持 Qt 6.2 可用和可维护 |

## 4. 代码结构设计

```text
apps/user/
├── main.cpp                         # 应用启动、配置、Mock 注入、REST 探测入口
├── user_main_window.*               # 页面容器、主流程状态与导航
├── user_main_window_login.cpp       # 手机号登录与验证码交互
├── user_main_window_navigation.cpp  # 路线方式与浏览器降级
├── user_main_window_orders.cpp      # 订单列表、空状态、小票入口
├── user_main_window_profile.cpp     # 头像、昵称、充值、退出
├── user_main_window_state.cpp       # 页面刷新、Toast、Esc 返回规则
├── user_demo_service.*              # 领域模型、Service 抽象、Mock 状态机
├── net/
│   ├── api_client.*                 # HTTP、Token、超时、统一响应信封
│   └── user_api.*                   # 唯一持有 /api/v1/user 路径的封装
└── ui/
    ├── app_theme.*                  # 统一配色、卡片和滚动条主题
    ├── bottom_navigation.*          # 首页/订单/我的 Dock 与微动效
    ├── station_list_widget.*        # 站点搜索、筛选、滚动列表
    ├── station_card.*               # 电站实体卡片
    ├── charger_table.*              # 电桩选择卡片
    └── charge_soc_gauge.*           # 自绘 SoC 环形仪表
```

数据路径应这样解释：

```text
Widget/Page → UserApi → ApiClient → HTTPS REST（正常模式）
Widget/Page → MockUserClientService（仅 --mock-scenario 演示/回归）
```

UI 只显示领域模型并发送用户操作；它不出现 SQL、JSON 字段名和 URL。`ApiClient` 负责统一添加 `Authorization`、`X-Request-ID`，写操作添加 `Idempotency-Key`，并将响应统一为 `ApiReply`。

## 5. 已完成阶段成果

1. 登录：11 位手机号校验、验证码倒计时、首次登录自动注册的演示语义；修复输入空格导致字符后移的问题。
2. 首页：当前位置、附近充电站卡片、关键字搜索、刷新、空状态。
3. 电站/电桩：展示距离、价格、空闲数、充电桩功率、类型、累计次数与状态；仅空闲桩可选。
4. 充电闭环：预约倒计时、取消、开始充电、实时电量/费用/功率/SoC、结算确认、小票。
5. 订单和账户：订单状态色、订单详情、充电主题空状态、昵称、头像、余额充值和退出登录。
6. 体验：首页/订单/我的三项 Dock、切换图标弹性反馈、细窄圆角滚动条、能源绿主题、站点一体化搜索区、Toast 自动淡入淡出和按需 `ⓘ` 帮助。
7. REST 基础：封装当前团队 V1 用户接口、Token、请求 ID、超时、头像 multipart 和幂等键；提供 `--api-request-code <phone>` 路由探测命令。

## 6. 当前限制，必须如实说明

- 窗口默认使用真实异步 REST；隔离服务端上的真实 HTTP 账户全流程已通过，但不应表述为“Qt 图形界面端到端录屏验收已完成”。
- `UserApi` 已直接被页面的异步回调调用；不能为了扩展功能改回 GUI 线程同步等待网络。
- 当前机器 CMake 为 3.22 且没有 Ninja，低于团队预设的 CMake 3.24/Ninja；已用隔离 CMake 严格编译（`-Werror`）与 Qt 离屏启动验证源码。

## 7. 创新点：答辩时作为“规划”表达

不要声称已经实现，建议说“下一阶段准备做”。

| 创新方向 | 用户价值 | 可实现方式 |
| --- | --- | --- |
| 智能选桩推荐 | 不是只按距离排序，而是结合空闲数、预计等待、价格和充电速度推荐 | 后端/ML 返回排序分与解释标签，客户端展示“推荐原因” |
| 充电预算与目标 SoC | 让用户在开始前理解大概成本、时长和电量目标 | 流程开始请求携带目标金额/余额下限，实时显示预测 |
| 低碳充电建议 | 引导用户在低碳/低峰时段充电 | 后端提供时段碳强度与价格，客户端展示可选建议 |
| 可信充电进度 | 断线后仍能恢复到真实流程 | 登录和重连调用 `GET /flows/active`，REST 快照与实时事件结合 |
| 无障碍与异常友好 | 降低老年用户/弱网用户操作成本 | 文字+颜色双重状态、可读错误、加载/空/失败/重试状态 |

## 8. PPT 可直接使用的文字

### 一页“用户端成果”

> 已完成 Qt Widgets 用户端主流程：登录、查站、选桩、预约、充电、结算、订单和账户管理。正常模式通过异步 HTTPS REST 接入后端，不直接访问数据库；`--mock-scenario` 保留为离线演示与回归模式。

### 一页“技术架构”

> 前端以 Qt Widgets 构建移动端交互模拟。正常模式由 QNetworkAccessManager 异步访问 `/api/v1/user/*`，统一处理 Token、超时、错误码和幂等键；Mock 仅由显式参数启用。订单、余额、计费和设备状态以服务端事务为唯一事实来源。

### 一页“下一阶段创新规划”

> 围绕“更省时、更省钱、更低碳”规划智能选桩推荐、预算/目标 SoC、低碳时段建议与断线流程恢复。前端负责解释和交互，后端/ML 负责数据、排序和计算，保证创新功能可验证、可落地。

## 9. 演示与截图清单

建议录制 60～90 秒或准备 4 张截图：

1. 登录页：品牌区、手机号输入和验证码提示。
2. 首页：附近站点、空闲资源和 Dock。
3. 选桩页：空闲/充电中/故障桩的可视化差异。
4. 充电控制页：SoC 环形仪表、时长、电量、费用与结算按钮。
5. 可选：结算小票或订单页，展示状态闭环。

推荐现场路径：启动本机服务端 → 输入 `13800138000` → 获取本次服务端返回的开发验证码 → 登录 → 选站和空闲桩 → 预约 → 开始充电 → 结束结算 → 查看订单。无服务端时使用 `--mock-scenario happy-path`，其演示验证码为 `123456`。

## 10. 高频提问与回答

**为什么保留 Mock？**  它保证离线演示和回归不受服务可用性影响；正常启动不使用它，避免演示数据替代真实业务事实。

**为什么客户端不直接连数据库？**  SQLite 的事务、余额扣减、并发抢桩和订单状态必须由服务端统一控制；直连会破坏权限边界和数据一致性。

**如何避免重复点击导致重复扣款？**  UI 先禁用结算按钮；客户端给写请求加 `Idempotency-Key`；服务端以同一键返回同一结算结果，三层共同保障。

**实时数据怎么实现？**  当前 Mock 用 QTimer 演示变化；正式模式每秒异步轮询 `GET /flows/{flowNo}/progress`，后续可消费 WebSocket 事件，但 REST 快照仍用于首屏和断线恢复。

**为什么不直接用成熟 UI 库？**  项目需要保证 Ubuntu Qt 6.2 可运行、依赖可控；因此使用自有 QSS/小组件实现一致的卡片、Dock、状态色和仪表，避免版本或授权风险。

**你的创新点落在哪里？**  不在客户端伪造结论：后端/ML 提供可解释的推荐、低碳和预测数据；客户端把推荐原因、预算和行动入口呈现给用户。
